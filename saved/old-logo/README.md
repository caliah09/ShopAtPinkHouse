# Old logo (superseded)

The "Shop PH by ZIONZAREA" lockup that was the site logo before the
"SHOP AT PH" monogram replaced it. Kept here in case it's wanted again —
copy `logo.webp` back to `assets/img/source/logo.webp` and re-run
`python3 tools/prepare_assets.py` to restore it.
